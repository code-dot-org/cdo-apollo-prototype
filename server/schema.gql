# -----------------------------------------------
# !!! THIS FILE WAS GENERATED BY TYPE-GRAPHQL !!!
# !!!   DO NOT MODIFY THIS FILE BY YOURSELF   !!!
# -----------------------------------------------

type Assessment {
  id: ID!
  name: String!
  script: Script!
}

type Mutation {
  markStudentAsssessmentCompleted(id: ID!): StudentAssessment!
  postStudent(student: StudentInput!): Student!
  setStudentAssessmentLocked(id: ID!, isLocked: Boolean!): StudentAssessment!
}

type Query {
  assessment(id: ID!): Assessment
  scriptById(id: ID!): Script
  sectionAssessments(assessmentId: ID!, sectionId: ID!): [StudentAssessment!]
  sectionById(id: ID!): Section
  studentById(id: ID!): Student
}

type Script {
  assessments: [Assessment!]!
  id: ID!
  name: String!
  students: [Student!]!
}

type Section {
  assignedScript: Script!
  id: ID!
  name: String!
  students: [Student!]!
}

type Student {
  age: Int
  assessments: [StudentAssessment!]!
  id: ID!
  name: String!
  section: Section!
}

type StudentAssessment {
  assessment: Assessment!
  id: ID!
  isCompleted: Boolean!
  isLocked: Boolean!
  student: Student!
}

input StudentInput {
  age: Int
  id: ID
  name: String!
}
